"""Evaluation harnesses (hit the real LLM; run manually, not in CI).

Unit tests in tests/ stay fast and offline; these measure AI quality against
labeled datasets — the parser (this milestone) and the scam detector (M3).
"""
